dist: xenial
language: python

python:
  - "3.7"

install:
  - pip install -r requirements.txt
  - pip install coveralls

script:
  - coverage run --source=authentication,device_controller,utils,rbp_controller manage.py test --settings=rbp_controller.settings.testing

after_success:
  - coveralls
